import streamlit as st

from core.db import init_db
from ui.rtl import apply_rtl
from ui.tenant_selector import render_tenant_selector

st.set_page_config(
    page_title="תזרים ועוד - דשבורד פיננסי",
    page_icon="📊",
    layout="wide",
    initial_sidebar_state="expanded",
)

init_db()
apply_rtl()
render_tenant_selector()

st.title("תזרים ועוד")
st.subheader("דשבורד ניהול חובות ותזרים מזומנים")

st.markdown(
    """
    כלי ליווי כלכלי לעסקים. מטרת הדשבורד: **לראות בכל רגע איפה העסק עומד**
    מבחינת חובות פתוחים, הכנסות צפויות והוצאות שוטפות, ולהבין מתי חוצים
    את הקו לתזרים בריא.

    ### דפי עבודה

    - **סקירה** — תמונת מצב כוללת: KPIs ועקומת תזרים מצטברת
    - **חובות** — רשימת החובות הפתוחים לפי גילאי איחור
    - **תזרים** — כניסות מול יציאות שבועי/חודשי
    - **הכנסות צפויות** — חוזים וחשבוניות עתידיות
    - **הוצאות שוטפות** — שכירות, משכורות, הוצאות קבועות
    - **לקוחות** — ניהול לקוחות (כעת לקוח אחד; הארכיטקטורה תומכת בריבוי לקוחות)

    בחר לקוח בסיידבר ועבור לדף הסקירה כדי להתחיל.
    """
)
